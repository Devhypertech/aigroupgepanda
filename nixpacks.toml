[providers]
node = "20"

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare npm@10.2.0 --activate",
  "npm install"
]

[phases.build]
cmds = [
  "npm run build:shared",
  "npm run build:railway:api",
  "npm -w @gepanda/api run build"
]

[start]
cmd = "npm -w @gepanda/api run start"
